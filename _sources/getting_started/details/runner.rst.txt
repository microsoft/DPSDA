Runner
======

API reference: :doc:`/api/pe.runner`.

:py:class:`pe.runner.pe.PE` manages the main **Private Evolution** algorithm by calling the other components discussed before. It has the following key methods:

* :py:meth:`pe.runner.pe.PE.run`: Runs the **Private Evolution** algorithm.
